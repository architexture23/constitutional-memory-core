name: Production Deploy (disabled pending revalidation)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Deployment intentionally disabled
        run: |
          echo "Automatic Railway/Vercel production deployment is disabled."
          echo "Re-enable only after the current public application boundary and deployment targets are explicitly revalidated."
