name: Auto-Release (disabled pending revalidation)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Release path intentionally disabled
        run: |
          echo "Automatic npm/PyPI publishing is disabled."
          echo "Re-enable only after package identity, credentials, build, clean-install, and release verification are explicitly restored."
